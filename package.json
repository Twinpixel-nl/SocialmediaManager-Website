{
  "name": "socialmediamanager",
  "version": "1.0.0",
  "description": "Website voor socialmediamanager.nl met blogfunctionaliteit",
  "scripts": {
    "generate": "node netlify/generate-blog.js",
    "build": "npm run generate && tailwindcss -i ./src/tailwind.css -o ./style.css",
    "dev": "tailwindcss -i ./src/tailwind.css -o ./style.css --watch"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.1"
  }
}
